"""Timers app package (currently unused).

Timer presets and ProPresenter timer control are handled by:
- webui.py routes (/timers, /api/timers, /api/timers/apply)
- cli.py commands (python cli.py timers ...)

This package exists only to preserve older import paths.
"""

